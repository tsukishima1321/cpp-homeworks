//实验六：二维数组、字符数组
//编写程序，将某一指定字符从一个已知的字符串中删除。假设已知字符串为“aaaasdfga”，将其中出现的'a'字母删除，删除后的字符串为“sdfg”。
#include <iostream>

int main()
{
    #ifdef _WIN32
    system("chcp 65001");
    #endif

    system("pause");
    return 0;
}